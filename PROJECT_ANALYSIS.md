# Анализ проекта ForteHackaton - Fraud Detection

## О чем проект

**Цель:** Построить конвейер детекции мошенничества (fraud) для мобильного банкинга:
- Загрузка и слияние данных
- EDA (исследовательский анализ)
- Feature Engineering
- Обучение модели CatBoost
- Оптимизация порога классификации
- Онлайн инференс (предсказания)

**Источники данных:** Два CSV в папке `docs/`:
- `транзакции в Мобильном интернет Банкинге.csv`
- `поведенческие паттерны клиентов.csv`

---

## Текущая структура проекта

### Основные компоненты

#### 📁 `src/train_catboost.py` (371 строка)
**Статус:** ✅ Наиболее полный модуль

**Что делает:**
- Загружает и чистит данные (обработка cp1251, переименование колонок, merge по `cst_dim_id` + `transdate`)
- Генерирует фичи:
  - Временные (hour, day_of_week, is_night)
  - Поведенческие (device_count, login patterns)
  - Агрегаты по пользователю (user_avg_amount, user_tx_count)
  - Композитные (amount_to_avg_ratio, login_velocity)
- Обучает CatBoost с опциональным grid search
- Оптимизирует порог классификации под максимальный F1-score
- Вычисляет важность фичей

**Проблемы:**
- ❌ Сохраняет модель в корень проекта (`catboost_fraud_final.cbm`)
- ❌ Не сохраняет список имен фичей (`feature_names.pkl`)

---

#### 📁 `src/predict.py` (214 строк)
**Статус:** ⚠️ Готов, но ждет артефакты

**Что делает:**
- Класс `FraudDetector` для real-time предсказаний
- Методы `predict_single()` и `predict_batch()`
- Определение уровней риска (LOW/MEDIUM/HIGH/CRITICAL)
- Рекомендации (APPROVE/REVIEW/ADDITIONAL_VERIFICATION/BLOCK)

**Ожидает:**
- ❌ `models/catboost_fraud_model.cbm` (модель)
- ❌ `models/feature_names.pkl` (список фичей)

**Проблемы:**
- Пороги риска (0.3/0.6/0.8) зашиты в коде
- Нет связи с `config.py`

---

#### 📁 `src/experiment_threshold.py` (195 строк)
**Статус:** ⚠️ Готов, но ждет модель

**Что делает:**
- Тестирует разные пороги классификации (0.2–0.6)
- Сравнивает метрики при разных порогах
- Находит лучший порог по F1-score

**Ожидает:**
- ❌ `models/catboost_fraud_model.cbm`

**Проблемы:**
- Дублирует логику загрузки данных из `train_catboost.py`

---

#### 📁 `src/config.py` (48 строк)
**Статус:** 💥 СЛОМАН

**Проблемы:**
- ❌ Невалидный Python (начинается с dict без имени переменной)
- ❌ Не импортируется
- ❌ Флаги `USE_GPU`, `USE_RAY` определены, но не используются

**Должен содержать:**
- Уровни риска и рекомендации
- Пороги классификации
- Целевые метрики
- Бизнес-метрики
- Гипер-параметры для grid search

---

#### 📁 `src/preprocessing.py` (7 строк)
**Статус:** 📝 Заглушка

**Содержит:**
- TODO: функции `load_data()` и `preprocess()`
- Не используется в коде

**Проблема:**
- Логика загрузки данных дублируется в каждом скрипте

---

#### 📁 `src/feature_utils.py` (14 строк)
**Статус:** 📝 Заглушка

**Содержит:**
- Placeholder функция `add_composite_features()`
- Возвращает df без изменений

---

#### 📁 `main.py` (187 строк)
**Статус:** ⚠️ EDA скрипт с проблемами

**Что делает:**
- Автопоиск и парсинг колонок
- Merge login-фичей
- Импутация, scaling, PCA
- KMeans кластеризация
- Генерация Jupyter notebook

**Проблемы:**
- ❌ Пишет в `/mnt/data` (Linux путь, на Windows упадет)
- ❌ Пути должны быть относительными

---

#### 📁 `models/`
**Статус:** 📂 Почти пусто

**Содержит:**
- `model_metrics.txt` (3 строки: дата, кол-во фичей)

**Отсутствует:**
- ❌ Файл модели
- ❌ Список фичей
- ❌ Подробные метрики

---

#### 📁 `requirements.txt`
**Статус:** ✅ Полный, но избыточный

**Содержит:**
- CatBoost, LightGBM, XGBoost
- SHAP, Optuna
- Streamlit, FastAPI
- Множество библиотек, которые пока не используются

---

## Основные проблемы

### 🔴 Критические

1. **config.py невалиден** — код не запускается
2. **Несогласованность артефактов**:
   - Training сохраняет: `catboost_fraud_final.cbm` (корень)
   - Inference ждет: `models/catboost_fraud_model.cbm` + `models/feature_names.pkl`
3. **Нет сохранения feature_names.pkl** — inference не запустится

### 🟡 Важные

4. **Дублирование кода загрузки данных** (train vs experiment)
5. **Платформенные проблемы путей** (`/mnt/data` на Windows)
6. **preprocessing.py не используется**
7. **Нет централизации порогов риска**

### 🟢 Желательно

8. Нет логирования
9. Нет unit-тестов
10. Нет веб-интерфейса (FastAPI/Streamlit в requirements, но не реализованы)
11. Минимальные метрики в model_metrics.txt

---

## План исправлений (пошагово)

### ✅ Шаг 1: Починить config.py — ВЫПОЛНЕНО
**Цель:** Сделать валидный Python модуль с централизованными настройками

**Что изменилось:**
- ✅ Определены все словари как переменные (RISK_LEVELS, RECOMMENDATIONS)
- ✅ Добавлены пороги риска THRESHOLDS = {low: 0.30, medium: 0.60, high: 0.80}
- ✅ Исправлены флаги USE_GPU=False, USE_RAY=False (не используются в коде)
- ✅ Файл теперь валидный Python модуль

**Файл:** `src/config.py`

---

### ✅ Шаг 2: Согласовать сохранение артефактов в train_catboost.py — ВЫПОЛНЕНО
**Цель:** Модель и фичи сохраняются туда, где их ждет predict.py

**Что изменилось:**
- ✅ Создание папки `models/` если не существует
- ✅ Сохранение модели как `models/catboost_fraud_model.cbm` (вместо корня)
- ✅ Сохранение списка фичей как `models/feature_names.pkl`
- ✅ Расширенный отчет по метрикам в `models/model_metrics.txt` (AUC, Precision, Recall, F1, Confusion Matrix, TOP-10 фичей)

**Файлы:** `src/train_catboost.py`

---

### ✅ Шаг 3: Реализовать централизованную загрузку данных — ВЫПОЛНЕНО
**Цель:** Убрать дублирование, единая функция для загрузки/чистки

**Что изменилось:**
- ✅ Реализована функция `load_data(base_path='docs')` — загрузка обоих CSV
- ✅ Реализована функция `clean_and_merge()` — чистка колонок, нормализация типов, merge
- ✅ Функция `clean_columns()` — удаление BOM и пробелов
- ✅ Обработка ошибок encoding, fallback пути
- ✅ Полное переименование русских колонок в английские короткие имена

**Файл:** `src/preprocessing.py` (206 строк кода вместо 7)

**Следующий шаг:** Использовать эти функции в `train_catboost.py` и `experiment_threshold.py`

---

### ✅ Шаг 4: Починить пути в main.py — ВЫПОЛНЕНО
**Цель:** Убрать `/mnt/data`, использовать локальные относительные пути

**Что изменилось:**
- ✅ Заменено `/mnt/data/` на `docs/` для всех выходных файлов
- ✅ Добавлена переменная `OUT_CLUSTER = "docs/cluster_summary_k4.csv"`
- ✅ Создание директорий перед записью (`os.makedirs()`)
- ✅ Исправлены пути в ноутбуках (f-строки вместо хардкода)

**Файл:** `main.py`

---

### ✅ Шаг 5: Централизовать пороги в predict.py — ВЫПОЛНЕНО
**Цель:** Использовать пороги из config.py, убрать хардкод

**Что изменилось:**
- ✅ Импортирован модуль `config`
- ✅ `_get_risk_level()` использует `config.THRESHOLDS['low/medium/high']`
- ✅ `_get_recommendation()` использует `config.THRESHOLDS['low/medium/high']`
- ✅ Единая точка управления порогами

**Файл:** `src/predict.py`

---

### ⏭️ Шаг 6: Подключить preprocessing.py в train и experiment — ОЖИДАЕТ
**Цель:** Использовать централизованную загрузку вместо дублирования

**План:**
- Заменить `load_and_clean_data()` в `train_catboost.py` на `from preprocessing import load_data, clean_and_merge`
- Заменить логику загрузки в `experiment_threshold.py` на импорт из `preprocessing`

---

### ⏭️ Шаг 7 (опционально): Добавить FastAPI endpoint — НЕ НАЧАТ
**Цель:** REST API для онлайн скоринга

**План:**
- Создать `src/api.py` с FastAPI приложением
- POST endpoint `/predict` для одной транзакции
- POST endpoint `/predict_batch` для нескольких
- Использовать `FraudDetector` из `predict.py`

---

### ⏭️ Шаг 8 (опционально): Добавить unit-тесты — НЕ НАЧАТ
**Цель:** Покрыть тестами ключевые функции

**План:**
- `tests/test_preprocessing.py`
- `tests/test_predict.py`
- `tests/test_features.py`

---

## Что работает сейчас

✅ **Тренировочный пайплайн** — наиболее полный:
- Загрузка/чистка данных
- Feature engineering
- Обучение CatBoost
- Оптимизация порога

✅ **Модуль инференса** — готов (при наличии артефактов):
- Класс FraudDetector
- Predict single/batch
- Risk levels

✅ **Эксперименты с порогами** — готов (при наличии модели)

---

## Что нужно для запуска

### Минимальный набор (после исправлений):

1. Положить данные в `docs/`:
   - `транзакции в Мобильном интернет Банкинге.csv`
   - `поведенческие паттерны клиентов.csv`

2. Установить зависимости:
   ```bash
   pip install -r requirements.txt
   ```

3. Обучить модель:
   ```bash
   python src/train_catboost.py
   ```

4. Запустить предсказания:
   ```bash
   python src/predict.py
   ```

5. Эксперименты с порогами:
   ```bash
   python src/experiment_threshold.py
   ```

---

## Следующие шаги развития

### Краткосрочные (1-2 дня)
- [ ] Исправить все критические проблемы (шаги 1-6)
- [ ] Протестировать полный цикл: train → predict → experiment
- [ ] Документировать формат данных

### Среднесрочные (неделя)
- [ ] Добавить FastAPI endpoint
- [ ] Streamlit дашборд для визуализации
- [ ] Unit-тесты (покрытие >70%)
- [ ] CI/CD pipeline

### Долгосрочные (месяц)
- [ ] Optuna для гиперпараметров
- [ ] SHAP explanations для каждого предсказания
- [ ] A/B тестирование разных моделей
- [ ] Мониторинг drift'а данных
- [ ] Docker контейнеризация

---

**Дата анализа:** 2025-11-27  
**Статус:** Готов к исправлениям
